// import PostsList from './components/PostsList'

function Dashboard() {
    //props
    // console.log("Inside Dashboard: ", { props });

    // const { posts } = props


    return (
        <main className="dashboard-layout">
            <section>
                <h2>Posts</h2>
                {/* {posts ? <PostsList posts={posts} /> : <p>Loading... Dashboard</p>} */}
            </section>
        </main>
    )
}

export default Dashboard